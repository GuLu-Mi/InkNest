# InkNest 开发协作规则

适用范围：本仓库的代码、文档、测试和构建配置。

## 修改前

- 阅读 `docs/README.md`、`docs/requirements.md`、`docs/architecture.md` 及相关专题。
- 以当前任务明确的范围为准，在变更说明中列出涉及的 REQ / NFR 编号。
- 发现文档与实现冲突时明确差异并统一说明，不静默改变产品行为。

## 产品与实现约束

- 目标平台为 Windows 11 x64、macOS 14 及以上 Apple Silicon Mac。
- 原始 Markdown 是正式内容，HTML 预览不能成为保存来源。
- 已有文件默认阅读；每标签独立状态，最多 20 个文档、一个活动视图，同一规范文件只有一个可写会话。
- 无账号、服务器或云同步；不扩展到知识库、协作、插件市场或所见即所得编辑。
- 使用 TypeScript strict。文件访问、写入、配置、恢复、历史与系统事件集中在主进程。
- preload 只暴露列明的业务接口，不提供通用 IPC、任意文件读写、Node 或命令执行能力。
- 保持 `nodeIntegration: false`、`contextIsolation: true`、`sandbox: true`、`webSecurity: true`。
- CodeMirror 持有可写编辑状态；Vue 不用另一份文本全量覆盖它。模式切换和保存不得清空撤销历史。
- 同文件写入串行、保存绑定快照、失败保留内容、冲突默认不覆盖。自动保存、恢复草稿、本地历史按 `docs/persistence.md` 分别实现。
- 使用现有解析器、编辑器和原子写入库；依赖精确锁定，安装使用锁文件，升级检查兼容性。

## 验证与文档

- 行为变更同步更新需求、交互、契约和相关测试；技术选择变化更新 `docs/decisions.md`。
- 保存、恢复、路径权限、编码和关闭流程必须有失败与时序测试。
- 纯文档变更检查内容、链接和一致性，不新增模拟实现的测试，不顺带修改应用。
- 区分自动化、原生平台和真实故障测试，构建成功不代表双平台可用。
- 测试使用隔离目录和固定样本，不改写用户真实 Markdown。
- 项目文档说明功能、使用方法、技术约束和已知限制；开发日志、执行计划、进度表、截图与运行报告只存于被忽略的 `docs/local/` 或 `.tooling/`。
- 完成任务时在回复中说明修改与实际验证结果，不向公开文档追加任务总结。

## 提交

- 检查差异和暂存文件，保留必要测试样本；不提交凭据、用户文档、恢复正文、依赖或构建产物。
- 本地验证通过后合入 `main`，GitHub 只推送 `main`，不上传临时分支。版本发布通过手动 Release 工作流完成。
- 不添加遥测、上传用户文档或擅自发布。分支与提交规则见 `CONTRIBUTING.md`。
