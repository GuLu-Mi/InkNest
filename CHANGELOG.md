# 版本变化

## 0.3.1

- 标签栏“+”改为返回首页，由用户选择打开或新建；已有文档保留编辑状态、撤销和阅读位置，后台保存与草稿备份继续。
- 首页左侧为“新建文档”、右侧为“打开文档”，移除标签栏重复的“打开…”按钮。
- 当前文档工具栏整合文件名、保存按钮及另存为/关闭下拉，仅在编辑模式显示保存操作；保存状态移至底部并跟随当前文档。
- 未选中标签增加短竖线分隔，选中标签保留独立圆角底色，悬停与键盘聚焦显示整块高亮；首页也能区分各个标签。
- 修复长文件名使“+”偏离标签末端的问题；标签溢出时隐藏滚动条，保留滚轮、触控板和键盘访问。
- 优化窄窗口及 200% 缩放下工具栏、操作菜单、状态栏和错误提示的布局。
- 修复手动保存等待输入法提交时切换标签或返回首页，可能将保存操作转给另一文档的问题；保存结果未确认时及时更新编辑保护状态。

平台验收、签名及仍待定位的问题见[已知限制](docs/known-issues.md)。

## 0.3.0

- 支持新建空白 Markdown 文档：欢迎页、标签栏“+”、文件菜单及 Cmd/Ctrl+N 直接进入编辑，保留独立的打开文件入口。
- 首次保存通过系统对话框选择位置，无扩展名时补充 `.md`，支持保存空文件；保存后保留当前标签、模式、光标和撤销历史。
- 未命名文档自动备份本地草稿，支持异常退出后分别恢复多份草稿；清空内容后同步更新备份，避免恢复已经删除的旧正文。
- 关闭非空未命名文档时提供保存、不保存、取消，保存成功后继续关闭；退出应用遇到未处理草稿时停止退出并定位该文档。
- 取消保存或写入失败时保留内容；保存结果暂时无法确认时，可重试核对，避免重复写入。
- 修复窄窗口及 200% 缩放下长文件名标签被裁切的问题。

平台验收、签名及内存占用问题见[已知限制](docs/known-issues.md)。

## 0.2.1

- 支持离线显示 Mermaid 图表与数学公式；图表可切换源码、复制代码和按原始大小查看，并适配浅色、深色主题。
- 新增常用语言代码高亮与复制、只读任务列表、脚注及返回链接、提示块、折叠内容和文档元数据。
- 补齐裸网址与邮箱链接、定义列表、Emoji 短码、高亮、上下标及少量 HTML 格式写法。
- 修复表格左、中、右对齐丢失；阅读、历史、恢复草稿预览和演示使用统一显示规则，搜索与目录同步适配。
- 图表或公式语法错误、受限或超限时保留源码并提示原因，其余正文继续显示；预览不改写 Markdown 原文。
- macOS 本地打包与测试结束后清理受管理构建副本的系统注册，避免在“打开方式”中残留重复候选。

支持语法与容量见 [Markdown 兼容性](docs/markdown.md)。不支持的私有语法、平台验收、签名及内存占用问题见[已知限制](docs/known-issues.md)。

## 0.2.0

- 图片查看器支持双指捏合连续缩放。
- 支持 macOS Command / Windows Ctrl + 滚轮缩放，普通滚轮保持滚动。
- 新增顺时针 90° 旋转按钮，仅调整查看方向，不修改图片文件。

## 0.1.0

- 查找与替换采用独立浮层和页签，开关面板不移动工具栏。
- 编辑模式支持当前匹配和全部字面替换，全部替换可一次撤销。
- 系统打开事件在界面就绪后交付，重复打开复用已有文档标签。

版本的功能与兼容性限制见[已知限制](docs/known-issues.md)。
